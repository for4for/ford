version: '3.8'

services:
  # PostgreSQL Database (Multi-tenant: ford ve tofas databases)
  postgres:
    image: postgres:16-alpine
    container_name: ford_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      # Multi-database init script will create both databases
      POSTGRES_MULTIPLE_DATABASES: ford_db,tofas_db
    ports:
      - "5484:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres-init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Django Backend (Multi-tenant)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ford_backend
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - DEBUG=${DEBUG:-1}
      - SECRET_KEY=${SECRET_KEY:-django-insecure-change-this-in-production}
      # Multi-tenant database configuration (tek user, iki database)
      - FORD_DB_HOST=postgres
      - FORD_DB_NAME=ford_db
      - FORD_DB_USER=postgres
      - FORD_DB_PASSWORD=postgres
      - FORD_DB_PORT=5432
      - TOFAS_DB_HOST=postgres
      - TOFAS_DB_NAME=tofas_db
      - TOFAS_DB_USER=postgres
      - TOFAS_DB_PASSWORD=postgres
      - TOFAS_DB_PORT=5432
      - ALLOWED_HOSTS=localhost,127.0.0.1,backend
      - CORS_ALLOWED_ORIGINS=http://localhost:3084,http://127.0.0.1:3084
      - FRONTEND_URL=${FRONTEND_URL:-http://localhost:5173}
      - EMAIL_BACKEND=${EMAIL_BACKEND:-django.core.mail.backends.console.EmailBackend}
      - EMAIL_HOST=${EMAIL_HOST:-smtp.gmail.com}
      - EMAIL_PORT=${EMAIL_PORT:-587}
      - EMAIL_USE_TLS=${EMAIL_USE_TLS:-True}
      - EMAIL_HOST_USER=${EMAIL_HOST_USER:-}
      - EMAIL_HOST_PASSWORD=${EMAIL_HOST_PASSWORD:-}
      - DEFAULT_FROM_EMAIL=${DEFAULT_FROM_EMAIL:-noreply@tofas.com.tr}
    ports:
      - "8084:8000"
    volumes:
      - ./backend:/app
      - media_files:/app/media
      - static_files:/app/staticfiles
    depends_on:
      postgres:
        condition: service_healthy

  # React Admin Frontend (Development with Hot Reload)
  frontend:
    image: node:20-alpine
    container_name: ford_frontend
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - VITE_API_URL=http://localhost:8084/api
    ports:
      - "3084:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  postgres_data:
  media_files:
  static_files:

