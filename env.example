# Django Backend Configuration
DEBUG=1
SECRET_KEY=django-insecure-change-this-in-production-use-random-string

# Database Configuration
POSTGRES_DB=ford_db
POSTGRES_USER=ford_user
POSTGRES_PASSWORD=ford_password

# Frontend Configuration
VITE_API_URL=http://localhost:8084/api
FRONTEND_URL=http://localhost:3084

# Email Configuration (for password reset)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For production, use SMTP:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@tofas.com.tr

# Production Settings (set DEBUG=0 for production)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Port Configuration (optional, docker-compose uses these)
# POSTGRES_PORT=5484
# BACKEND_PORT=8084
# FRONTEND_PORT=3084

